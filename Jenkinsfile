pipeline {
    agent any
    stages {
      stage('build') {
        steps {
          echo 'Generating Build'
        }
      }

      stage('test') {
        steps {
          echo 'Generating Test'
        }
      }

    }
}